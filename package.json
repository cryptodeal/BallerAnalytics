{
  "name": "balleranalytics",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "pnpm recursive run build --stream",
    "dev": "pnpm recursive run dev --stream",
    "format": "pnpm recursive run format",
    "lint": "pnpm recursive run lint",
    "mtgen": "pnpm recursive run mtgen",
    "preview": "pnpm recursive run preview",
    "data:cleanup:sortSeasons": "pnpm recursive run data:cleanup:sortSeasons",
    "data:import:boxscores": "pnpm recursive run data:import:boxscores",
    "data:import:teamRosters": "pnpm recursive run data:import:teamRosters",
    "data:import:playerData": "pnpm recursive run data:import:playerData",
    "data:cleanup:Game2:clearReferences": "pnpm recursive run data:cleanup:Game2:clearReferences",
    "data:cleanup:Game2:fixTeamPeriodStats": "pnpm recursive run data:cleanup:Game2:fixTeamPeriodStats",
    "test": "uvu -r tsm packages tests",
    "test:coverage": "pnpm recursive run test:coverage --stream"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,ts,tsx}": [
      "eslint --fix"
    ]
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.19.0",
    "@typescript-eslint/parser": "^5.19.0",
    "eslint": "^8.13.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.0.0",
    "husky": "^7.0.4",
    "lint-staged": "^12.3.7",
    "prettier": "^2.6.2",
    "tsm": "^2.2.1",
    "typescript": "^4.6.3",
    "uvu": "^0.5.3"
  }
}